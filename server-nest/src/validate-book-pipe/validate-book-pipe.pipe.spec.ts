import { ValidateBookPipePipe } from './validate-book-pipe.pipe';

describe('ValidateBookPipePipe', () => {
  it('should be defined', () => {
    expect(new ValidateBookPipePipe()).toBeDefined();
  });
});
